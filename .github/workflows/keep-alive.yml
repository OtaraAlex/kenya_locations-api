name: Keep Alive

on:
  schedule:
    - cron: '0 12 */6 * *'  # every 6 days at noon

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API
        run: curl https://your-render-app.onrender.com/health